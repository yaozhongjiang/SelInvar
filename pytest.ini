# `pytest` with no arguments collected code/legacy/, whose modules import a
# package layout that no longer exists, so a reviewer's first command failed on
# code the paper does not use. The suite lives in tests/ and is what `make test`
# runs; this makes the bare invocation do the same thing.
[pytest]
testpaths = tests
norecursedirs = legacy outputs data .*
